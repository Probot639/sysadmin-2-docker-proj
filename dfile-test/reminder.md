# Showing stuff off:
- curl http://localhost:8080
- docker network inspect bridge-net
- curl http://localhost:9100/metrics
- curl http://192.168.1.200
- docker logs offline-worker
- docker exec -it app-bridge sh
- and ^^ ping db-bridge
